# Agent 工作流改进记录

状态：本轮完成 · 2026-09-12。范围为项目指令、文档状态和只读账号诊断，不修改会员产品行为，不提交、推送或部署。

依据：https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra

## 计划与验收

- [x] 在项目规则中明确通用技能按需使用，保留用户流程及权限边界；不修改插件缓存或其他项目的配置。
- [x] 根 AGENTS.md 改为任务导航，游戏规则原文迁入 IHassistant/AGENTS.md，保留全项目游戏任务的显式路由。
- [x] 标记历史 onboarding 设计与验证；记录当前注册保存资料同时进入 pending 的实际行为。
- [x] 增加只读账号诊断 CLI，限制输入/输出并验证失败路径、只读事务与查询结果。
- [x] 检查相关文档链接、差异及代表性任务路由，记录实际验证边界。

## 工作区边界

开始时已有 IHassistant 两份 README、docs/guide-translations.md、游戏知识索引和制图技能的未提交修改，以及周攻略等未跟踪产物。本任务不清理这些文件；游戏规则只是迁移到未被索引的指令文件，不重新生成现有游戏索引。

## 验证结果

- 10 项合成测试通过，覆盖输入限制、凭证异常脱敏、只读连接/事务、环境不匹配拒绝、旧身份映射及聚合查询。
- 2026-09-12 19:11 UTC 实际执行 production 汇总及用户指定的旧账号查询，两次均通过服务器只读状态和环境校验，并返回结果；没有业务写入。没有将单账号生产数据写入本记录。
- 文档相对链接检查和 `git diff --check` 通过。游戏规则逐条对照原文件：仅将规则所在文件的说明改为新位置，其他游戏规则保留。
- 人工检查任务路由：账号排查进入权限文档和诊断工具；普通文案小修无需游戏资料或规划门槛；计算器规则进入游戏指令和对应计算文档；周攻略仍进入固定来源制图技能；发布仍须当次明确授权。
- 上述路由为静态审阅，未进行模型效果或耗时的 A/B 测量。会员入口、角色策略和 Admin 统计行为未改动，未作新的页面或登录流程验收。

本轮未提交、推送或部署；产品层面的会员入口和统计语义修复应作为明确的产品变更处理。
